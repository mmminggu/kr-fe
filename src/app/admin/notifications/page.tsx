'use client';

import React, { useState } from 'react';
import Notifications from '@/src/components/admin/notifications/Notifications';

export default function NotificationsPage() {

    return (
        <Notifications />
    );
}